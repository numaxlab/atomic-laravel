# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.0.0-beta.1] - 2026-04-25

### Added
- Atom components: `button`, `input`, `select`, `textarea`, `forms/*`
- Molecule components: `banner`, `breadcrumb`, `summary`
- `AtomicServiceProvider` with anonymous component path registration
- View publishing via `numaxlab-atomic` tag

### Fixed
- Corrected `publishes()` source path in `AtomicServiceProvider`
